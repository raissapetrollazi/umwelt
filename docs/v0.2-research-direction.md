# v0.2 Experiment - Spatial Mouse

## Status

Umwelt v0.2 is the first spatial milestone. Its current foundation implements
explicit spatial observations, rectangular arena geometry, gap-aware trajectory
measurements, and a pinned recorded-data adapter. This document defines the
scientific question, dataset boundary, information categories, remaining
evaluation plan, and implementation constraints for completing the experiment.

Version 0.2 does not yet implement a generative movement baseline or a
recorded-versus-synthetic spatial comparison. It does not claim a validated
model of mouse navigation, exploration, anxiety, motivation, or
decision-making. The implemented foundation introduces an explicit spatial body
and world so that recorded and synthetic trajectories can eventually be
represented and compared reproducibly.

## Core question

> Can a compact, explicit generative model reproduce selected geometric and
> kinematic properties of recorded mouse trajectories in an open field?

The question is deliberately narrower than explaining why a mouse moves as it
does. Version 0.2 asks whether a small executable movement model can reproduce
measured spatial structure. It does not infer intention or recover a biological
navigation mechanism.

## Selected source dataset

The selected source is the public Zenodo deposit:

- title: *Raw video and pose estimation data of top view open field mouse
  behavior recordings after yohimbine injections*;
- authors: Lukas M. von Ziegler, Fabienne K. Roessler, Oliver Sturman, Eoin C.
  O'Connor, and Johannes Bohacek;
- record DOI: `10.5281/zenodo.8188683`;
- source context: one Roche open-field experiment containing 32 unique animals
  and recordings: eight `Control` records at dose `0` and eight `Yohimbine`
  records at each of doses `1`, `3`, and `6` mg/kg;
- selected archive: `data.zip`, containing DeepLabCut pose-estimation CSV data;
- published archive size: `514028268` bytes;
- published MD5 for `data.zip`: `8b48f2060146c03d578d5d6971aa70e9`;
- metadata file: `METADATA_ROCHE.csv`;
- published metadata size: `6266` bytes;
- published MD5 for `METADATA_ROCHE.csv`:
  `096e21e4d319130370aa4bb244b670f8`;
- data license: CC BY 4.0.

Raw videos are not required for the first v0.2 experiment and should not be
downloaded by default. The pose archive is sufficient for the planned
trajectory-first workflow. The adapter pins the published byte size and MD5 for
both source files, enforces the metadata checksum during cataloging, and exposes
separate archive verification because `data.zip` need not remain after
extraction. It also validates the metadata schema, metadata-to-file mapping,
DeepLabCut header layout, frame indices, and pinned point names. [Local data
instructions](../data/README.md) document the download, checksums, and extraction
layout.

Zenodo publishes integrity values for `data.zip`, not for its individual CSV
members. Umwelt carries a 32-entry manifest derived locally after the archive
matched Zenodo's published size and MD5. Each entry pins one animal/file pair,
the extracted byte size, exact individual frame count, and SHA-256. The 32
counts total 1,726,595 frames and range from 53,914 to 53,964, but validation
uses each exact entry rather than accepting any count within that range. These
per-file SHA-256 values and counts are derived Umwelt provenance, not values
published by Zenodo.

The 17 tracked points have two distinct roles. Thirteen are mouse-body
keypoints: `nose`, `headcentre`, `neck`, `earl`, `earr`, `bodycentre`, `bcl`,
`bcr`, `hipl`, `hipr`, `tailbase`, `tailcentre`, and `tailtip`. Four are arena
landmarks: `tl`, `tr`, `bl`, and `br`. Umwelt keeps the arena landmarks separate
from `Pose2D`; they must not inflate mouse-pose counts or be selected as an
animal trajectory.

The dataset contains pharmacological treatment groups, but v0.2 is not a drug-
effect experiment. Treatment and dosage remain recorded provenance. They must
not be silently converted into behavioral causes or internal model variables.

## Primary analysis population

The first spatial baseline uses exactly the eight control recordings so the
core movement experiment does not pool known pharmacological interventions.

The adapter verifies that the metadata contains 32 unique animal identifiers and
32 unique DLC filenames, with one recording per listed animal. The recorded
contract selects the eight records whose source categories are exactly
`Control` and dose `0`, then splits by animal identity. It ranks the SHA-256 of
the UTF-8 payload
`umwelt-v0.2-control-split-v1|096e21e4d319130370aa4bb244b670f8|{animal_id}`;
the two smallest digests become development animals. The metadata MD5 in that
payload is the checksum published by Zenodo and verified by the adapter.

The resulting role assignment is derived without using trajectory coordinates
or spatial summaries:

- fitting: `16459-67049`, `16459-67053`, `16459-67060`, `16459-67067`,
  `16459-67071`, and `16459-67074`;
- development: `16459-67064` and `16459-67078`.

The pinned protocol facts in `umwelt.spatial_protocol` preserve the split
derivation and exact identifiers. A metadata change or missing control animal
must stop the experiment rather than silently produce a new split. Development
trajectories must not tune the baseline and remain development data rather than
a pristine confirmatory test. With only two development animals, their eventual
comparison is descriptive model-development evidence, not a population-level or
confirmatory test.

The yohimbine recordings may later support an explicitly declared
out-of-distribution diagnostic. They are not required for the first spatial
baseline and must not be used to claim causal treatment effects.

## Recorded information

The source pose files contain frame-indexed position estimates produced by
DeepLabCut. Umwelt preserves the distinction between source observations and
quantities derived from them.

The validated recorded input includes:

- sequential frame indices beginning at zero, without treating them as elapsed
  seconds;
- `(x, y)` pixel coordinates in an image frame where `+x` points right and `+y`
  points down, for 13 tracked mouse-body keypoints;
- coordinates in the same image frame for four separately represented arena
  landmarks;
- per-point DeepLabCut likelihood values;
- source animal and recording identifiers;
- experimental group, dosage, and source video filename metadata;
- missing source coordinates.

A DeepLabCut coordinate is a measurement produced by a pose-estimation system.
It is not a direct physical-state measurement and must retain that provenance.
Rejecting a coordinate under an explicitly selected likelihood threshold creates
a derived gap; it does not rewrite the source estimate as absent.

The first recorded protocol selects the source `bodycentre` keypoint and sets
`minimum_likelihood` explicitly to `None`. The available source material does
not validate a cutoff, so Umwelt does not invent one. Finite coordinates remain
accepted even when their likelihood is low or missing; the source likelihood is
still preserved for quality control. This rule is a documented limitation, not
a claim that low-likelihood coordinates are reliable or that DeepLabCut
likelihood is a calibrated probability.

The protocol itself applies no interpolation or smoothing. The later workflow
must feed it adapter frames directly and record any upstream transformation;
the protocol cannot infer whether an arbitrary caller already modified frames.
It requires accepted endpoints at adjacent frame indices for displacement. A
missing pose, missing coordinate, rejected coordinate, or skipped frame breaks
continuity. Exact zero displacement is retained as an observation and breaks the
turning sequence; turning resumes only after two consecutive nonstationary valid
movements. The canonical policy also rejects synthetic source contexts and
coordinate frames other than the pinned Roche image frame.

## Derived spatial information

Quantities computed by Umwelt from accepted source coordinates are derived
measurements, not newly recorded observations. The current foundation can
derive:

- representative body position while preserving subject, recording, recorded or
  synthetic source category, coordinate unit, and axis orientation in a shared
  `SpatialContext`;
- a `PositionRule` recording the selected keypoint and optional likelihood
  threshold, plus a `PositionStatus` that distinguishes missing source pose,
  missing source coordinate, and analytical rejection without discarding the
  original coordinate or likelihood;
- displacement between valid adjacent observations;
- a typed path-length result that retains its position rule and `SpatialContext`
  and exposes the coordinate unit;
- movement heading normalized to counterclockwise radians from `+x`, including
  sign correction for the Roche `x-right-y-down` image frame;
- turning angle between valid adjacent movements.

Later experiment work may add:

- speed if a validated time scale becomes available;
- anatomical orientation when a declared keypoint rule supports it;
- distance to arena boundaries when arena geometry is known;
- spatial occupancy summaries.

Gaps must remain explicit. Umwelt must not calculate displacement, speed, or
turning across missing intervals as if the trajectory were continuous.

Physical units must never be fabricated. If the source supports only pixel
coordinates, the adapter preserves pixel-space geometry. Conversion to
centimeters requires explicit calibration or documented arena geometry.

## Spatial representation

Version 0.2 has introduced a small headless spatial vocabulary required by this
experiment:

- finite two-dimensional coordinates;
- a coordinate frame with explicit identifier, unit, and axis orientation;
- frame-indexed spatial series with subject, recording, source category, and
  explicit gaps;
- a reusable spatial context carried from a series into representative positions,
  trajectory steps, and path length so recordings, sources, or coordinate frames
  cannot be mixed silently;
- a minimal body pose, separately represented arena landmarks, and a
  representative position;
- explicit arena bounds;
- derived trajectory and kinematic measurements.

These concepts should not be coupled to a renderer. A complete v0.2 experiment
must be runnable and inspectable without graphics.

## Arena boundary

The open-field arena is part of the experimental context, not merely a drawing
surface. Umwelt represents axis-aligned rectangular bounds in an explicit
coordinate frame. In the Roche adapter, the four source arena landmarks remain
separate from the mouse pose and can support image-space bounds without claiming
physical dimensions.

The implementation must not guess real-world dimensions from image dimensions.
If only image-space bounds are supported by the source, the model and metrics
must state that they operate in image coordinates.

Version 0.2 may use regions such as center and periphery only as deterministic
geometric partitions used for measurement. It must not interpret time in those
regions as anxiety, fear, confidence, preference, or motivation.

## Initial evaluation dimensions

Recorded and synthetic trajectories should be summarized separately before any
comparison. The first evaluation set should remain compact and inspectable.
Candidate dimensions are:

- total path length;
- displacement distribution;
- speed distribution where time calibration is valid;
- turning-angle distribution across valid contiguous, nonstationary movements;
- distance-to-boundary distribution;
- center/periphery occupancy under an explicitly defined geometric partition;
- coarse two-dimensional occupancy profile;
- movement autocorrelation at declared lags when sampling supports it.

The exact metric set should be declared before inspecting synthetic baseline
results. No implicit scientific acceptance threshold should be introduced.

## Minimal generative baseline

No v0.2 generative model is implemented yet. The first model should be an
explicit, seeded movement baseline rather than machine learning. It should
generate new trajectories rather than classify or replay recorded ones.

A suitable minimal family may condition the next displacement on a small set of
observable quantities such as recent movement magnitude, recent heading, and
arena geometry. The exact mechanism belongs to the later modeling commit and
must be justified against the recorded measurements available from the selected
adapter.

The model must not contain variables named as emotions, intentions, anxiety,
curiosity, goals, or other psychological constructs merely to improve fit.

## Relationship to v0.1

Version 0.2 extends the laboratory architecture rather than pretending that the
COMPASS temporal experiment contains spatial information that was never
recorded.

The v0.1 sleep/activity models and the v0.2 spatial model may use different
source datasets, sampling rates, observation schemas, and scientific questions.
They should still share project principles:

- explicit source categories;
- strict validation;
- reproducible configuration;
- deterministic seed derivation;
- recorded and synthetic evaluation kept separate until comparison;
- provenance for every transformation;
- negative and null results preserved;
- model complexity added only in response to a defined question or documented
  baseline failure.

The v0.2 baseline does not need to combine sleep/activity state with movement.
Multimodal integration remains a later milestone unless a concrete experiment
requires it.

## Explicit non-goals for v0.2

Version 0.2 does not introduce:

- causal environmental interventions;
- food, shelter, light, or stimuli as decision-driving world state;
- social interaction between synthetic animals;
- pharmacological causal-effect modeling;
- neural or physiological modalities;
- latent mental-state inference;
- counterfactual branching;
- graphical rendering as a simulation requirement;
- a general physics engine;
- a claim that geometric similarity demonstrates biological mechanism recovery.

Those boundaries keep v0.2 distinct from the planned Behavioral World, Social
Animals, Neural Mouse, latent-state, and counterfactual milestones.

## Reproducibility requirements

A completed spatial run should preserve enough information to reconstruct what
was measured, fitted, generated, and compared. At minimum this is expected to
include:

- dataset DOI, authorship, license, selected file names, published byte sizes,
  and published MD5 checksums;
- the provenance boundary between Zenodo-published file integrity and per-pose
  sizes, exact frame counts, and SHA-256s derived from the verified archive;
- resolved recording/animal roles;
- source coordinate semantics and accepted keypoints;
- pose-confidence and missing-data rules;
- coordinate-frame identifier, unit, and axis orientation;
- subject, recording, source category, and coordinate context retained by
  trajectory measurements;
- arena geometry source;
- derived-measurement definitions;
- complete fitted model parameters and assumptions;
- master and derived seeds;
- recorded and synthetic metrics as separate categories;
- readable comparison and limitations;
- software version or Git revision;
- artifact hashes.

Downloaded source data remains local and should not be committed to the Umwelt
repository.

## Stop conditions for the remaining experiment

The implementation should stop for design review rather than inventing missing
scientific facts if any of the following occur:

- a leakage-safe control fitting/development split cannot be justified from the
  eight available control animals;
- a future pose source or transformation cannot preserve the currently verified
  distinction between body keypoints and arena landmarks;
- sampling rate or time calibration required by a planned metric cannot be
  established;
- arena geometry required for boundary metrics cannot be verified;
- the selected dataset license or attribution requirements cannot be preserved;
- a proposed abstraction requires assumptions not supported by the concrete
  experiment.

These are experiment-design failures to resolve explicitly, not conditions that
should be hidden by permissive parsing or guessed constants.
